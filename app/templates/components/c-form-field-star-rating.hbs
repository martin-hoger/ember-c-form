{{#c-form-field
  label=label
  description=description
  errorMessage=errorMessage
  model=model
  field=field
  required=required
  class=class
}}
  {{!-- {{input value=(mut (get model field)) disabled=(if disabled "disabled" "")}} --}}
  {{c-star-rating value=(mut (get model field)) editable=true}}
  {{!-- {{c-star-rating value=model.rating editable=true onSelect=(action "onSelect")}} --}}
{{/c-form-field}}
